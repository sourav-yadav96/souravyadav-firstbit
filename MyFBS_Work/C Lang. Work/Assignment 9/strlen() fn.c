#include <stdio.h>
#include <string.h>

void main()
{
    char str[30] = "Firstbit Solution";
    int length;
    length = strlen(str);
    printf("The given String is: %s\n", str);
    printf("The Length of string is: %d\n", length);
}
